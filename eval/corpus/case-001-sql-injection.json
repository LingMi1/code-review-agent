{
  "id": "case-001",
  "name": "SQL Injection — raw string concatenation",
  "category": "security",
  "language": "go",
  "diff": "diff --git a/user_handler.go b/user_handler.go\nnew file mode 100644\n--- /dev/null\n+++ b/user_handler.go\n@@ -40,0 +42,7 @@\n+\n+func getUser(db *sql.DB, name string) (*User, error) {\n+\tquery := \"SELECT * FROM users WHERE name = '\" + name + \"'\"\n+\trow := db.QueryRow(query)\n+\tvar u User\n+\terr := row.Scan(&u.ID, &u.Name, &u.Email)\n+\treturn &u, err\n+}\n"
}
